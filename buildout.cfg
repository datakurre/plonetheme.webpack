[buildout]
extends = https://dist.plone.org/release/5-latest/versions.cfg
parts = instance

[instance]
recipe = plone.recipe.zope2instance
user = admin:secret
eggs = Plone
resources = ${buildout:directory}/build
